pyspark==3.5.1
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
# torchvision intentionally NOT pinned here — deploy/Dockerfile installs
# torch+torchvision together from the CUDA wheel index (cu126). Pinning
# torchvision==0.17.0 here used to force pip to downgrade torch 2.6.0 -> 2.2.0
# on `pip install -r requirements.txt` (torchvision 0.17.0 requires torch==2.2.0
# exactly). Do not re-add a torchvision pin without also removing/matching the
# Dockerfile's own torch/torchvision install.
ultralytics==8.3.40
matplotlib==3.9.2
tabulate==0.9.0
boto3==1.35.99

# This file is the CORE / shared dependency set only — installed in the base
# image (deploy/Dockerfile) that every model image is built from. Pipeline
# plugins with their own extra deps (e.g. ner_translate needs gliner,
# transformers, OCR libraries) do NOT belong here — see
# models/pipelines/<name>/requirements.txt and deploy/Dockerfile.<name>
# instead. This keeps one pipeline's deps from ever silently upgrading/
# downgrading a package shared by every other model (see
# docs/MODEL_CONTAINER_ISOLATION.md for the incident that motivated this).
