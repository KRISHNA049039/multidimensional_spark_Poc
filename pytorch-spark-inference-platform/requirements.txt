pyspark==3.5.1
numpy==2.1.3
pandas==2.2.3
pyarrow==18.1.0
# torchvision intentionally NOT pinned here — deploy/Dockerfile installs
# torch+torchvision together from the CUDA wheel index (cu126). Pinning
# torchvision==0.17.0 here used to force pip to downgrade torch 2.6.0 -> 2.2.0
# on `pip install -r requirements.txt` (torchvision 0.17.0 requires torch==2.2.0
# exactly). Do not re-add a torchvision pin without also removing/matching the
# Dockerfile's own torch/torchvision install.
ultralytics==8.3.40
matplotlib==3.9.2
tabulate==0.9.0
boto3==1.35.99

# --- ner_translate pipeline deps (models/pipelines/manifest.json extra_requirements) ---
# transformers capped <5.0: mt_ner_all_formats.py's NLLB tokenizer/generate calls
# target the 4.x API; transformers 5.x is a breaking rewrite.
gliner==0.2.13
transformers==4.57.6
sentencepiece==0.2.0
py3langid==0.4.0          # requires numpy>=2.0 -- keep in sync with numpy pin above
# Multi-format document extraction (extract_text() dispatch in mt_ner_all_formats.py)
pypdf==5.1.0
pdf2image==1.17.0
pillow==11.0.0
pytesseract==0.3.13
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
odfpy==1.4.1
ebooklib==0.18
beautifulsoup4==4.12.3
striprtf==0.0.28
xlrd==2.0.1
